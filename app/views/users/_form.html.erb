<%= simple_form_for @user, defaults: { wrapper_html: { class: 'form-group' }, input_html: { class: 'form-control' } }, html: { multipart: true, novalidate: true } do |f| %>
  <%= f.input :name %>
  <%= f.input :email %>

  <%= f.submit %>
<% end %>